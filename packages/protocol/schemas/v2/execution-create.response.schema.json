{
  "$id": "goyais.protocol.v2.execution-create.response",
  "type": "object",
  "required": ["execution_id", "trace_id", "session_id", "state"],
  "properties": {
    "execution_id": { "type": "string", "minLength": 1 },
    "trace_id": { "type": "string", "minLength": 1 },
    "session_id": { "type": "string", "minLength": 1 },
    "state": {
      "enum": ["pending", "executing", "waiting_confirmation", "completed", "failed", "cancelled"]
    }
  },
  "additionalProperties": false
}
