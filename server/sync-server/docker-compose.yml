services:
  sync-server:
    build: .
    ports:
      - "8140:8140"
    environment:
      SYNC_SERVER_TOKEN: change-me
      SYNC_SERVER_DB_PATH: /data/sync-server.db
    volumes:
      - sync-data:/data

volumes:
  sync-data:
