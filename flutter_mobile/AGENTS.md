# flutter_mobile AGENTS (Design Phase)

当前模块处于设计阶段（design phase）。编码前必须先通过设计门禁。

## Design Gate (MUST)

- 输出信息架构、页面流程、状态管理方案。
- 输出接口契约映射（与后端 messageKey/error model 对齐）。
- 输出 DoD、测试矩阵（含离线/弱网/权限场景）、回滚策略。
- 与 `docs/prd.md` 和 Web 交互口径保持一致。

## Ready for Coding 条件

- 核心交互流与接口契约评审通过。
- 风险与 fallback 路径明确。
